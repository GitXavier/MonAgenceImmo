{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Gérer les nouveaux biens</h1>

        <table class="table table-striped">
            <thead>
            <tr>
                <th>Titre</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for property in properties %}
            <tr>
                <td>{{ propriete.titre }}</td>
                <td>
                    <a href="{{ path('property_edit', {id: propriete.id}) }}" class="btn btn-default">Editer</a>
                </td>
            </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>



{% endblock %}
